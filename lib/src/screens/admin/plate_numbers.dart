import 'package:flutter/material.dart';

class AllowedPlateNumbersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Allowed Plate Numbers'),
      ),
      body: const Center(
        child: Text('Allowed Plate Numbers Page'),
      ),
    );
  }
}
