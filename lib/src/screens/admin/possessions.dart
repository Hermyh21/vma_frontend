import 'package:flutter/material.dart';

class AllowedPossessionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Allowed Possessions'),
      ),
      body: const Center(
        child: Text('Allowed Possessions Page'),
      ),
    );
  }
}
